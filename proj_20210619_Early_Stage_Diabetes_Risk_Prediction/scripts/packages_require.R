# Carrega Pacotes

require(moments) # Coeficiente de Assimetria

require(rvest) # %>% - Concatenação no uso de funções

require(fdth) # Tabelas de distribuição de frequências

require(tidyverse) # Para função tibble()

require(gmodels)

require(mice)

require(psych) # Scatterplot Matrix

require(stargazer)

require(ROCR) # Gerando uma curva ROC em R

require(e1071)

require(caret) # Gerando Confusion Matrix com o Caret

require(Amelia) # missmap - Mapa de valores missing

require(xtable)

require(DT)

require(knitr)
